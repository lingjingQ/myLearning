package leetCode;

import java.util.List;

/**
 * @athor LingjingQ
 * @date 2019/4/14 10:25
 */

    class 求超过三分之一的数 {
        public List<Integer> majorityElement(int[] nums) {
            //超过1/3的数，最多只有两个，我们遍历一遍所有数，一定可以找出两个候选的数num1，num2,其计数分别为count1,count2。
            int num1,num2,count1,count2;
            for(int i=0; i<nums.length; i++){
                // 拿到一个num[i] ,首先将其和num1比较，看能不能作为1号候选，如果能，就作
            }
        }
    }

